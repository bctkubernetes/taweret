# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

dist/

# macOS Finder metadata. Never useful in a repo, and it appears in every directory
# you happen to open in Finder — .DS_Store and .lighthouse/.DS_Store were both sitting
# untracked here. Ignored rather than committed.
.DS_Store
**/.DS_Store
